spring.application.name=traiviu-backend

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/traiviu}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:iaruni}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8085
server.address=0.0.0.0
server.ssl.enabled=false
server.forward-headers-strategy=framework

app.jwt.secret=${APP_JWT_SECRET:dGhpc19pc19hX3ZlcnlfbG9uZ19zZWNyZXQxMjM0NTY=}
app.jwt.expiration=${APP_JWT_EXPIRATION:86400000}

app.upload.dir=${APP_UPLOAD_DIR:/app/uploads}
app.public-base-url=${APP_PUBLIC_BASE_URL:https://192.168.7.155:8000}
app.cors.allowed-origins=${ALLOWED_ORIGINS:https://localhost,http://localhost,http://localhost:8100,http://localhost:4200,https://ivani26.iesmontenaranco.com}